# Bun configuration for Angular + Rspack project

[install]
# Use Bun's native installer
prefer-offline = true

[run]
# Use Bun's JavaScript runtime
shell = "bun"

[test]
# Test configuration - run only the consolidated test file
root = "./src/app"
